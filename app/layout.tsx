import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import { Analytics } from "@vercel/analytics/next"
import { Suspense } from "react"
import "./globals.css"

export const metadata: Metadata = {
  title: "Salmans Portfolio",
  description:
    "Professional portfolio of an iOS developer and aspiring data scientist specializing in AR learning apps and ML projects. Based in India, open to relocation to Dubai, UAE.",
  keywords:
    "iOS developer Dubai, SwiftUI developer UAE, AR iOS developer, Data Science AI ML Dubai, mobile app developer",
  generator: "v0.app",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable}`}>
        <Suspense fallback={<div>Loading...</div>}>
          {children}
          <Analytics />
        </Suspense>
      </body>
    </html>
  )
}
